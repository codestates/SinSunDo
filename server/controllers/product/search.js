const { product } = require('../../models/food')
const { food_img } = require('../../models/food_img')
const { category } = require('../../models/category')
const { expiration } = require('../../models/expiration')

module.exports = (req, res) => {

}